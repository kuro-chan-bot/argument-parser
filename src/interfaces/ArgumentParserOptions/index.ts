/*
 * Argument parser options.
 */
export interface ArgumentParserOptions {
  stringChars: string[]
  escapeChars: string[]
  separatorChars: string[]
  trueStrings: string[]
  falseStrings: string[]
}
